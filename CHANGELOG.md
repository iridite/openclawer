# Changelog

## v1.2.1 - 2026-03-17

### Highlights

- 强化管理面板与静态资源的 HTTP 交付链路，默认按远程访问场景运行，并优化被策略拦截后的错误提示。
- 修复 QQ Bot / 企业微信插件安装后的运行时识别、启用与配置写回问题，避免把 npm 包名错误写入 `plugins.allow`。
- 增强模型测试可观测性，除了请求预览外，新增底层运行时执行细节，便于排查真实请求行为。
- 统一模型卡片、消息渠道卡片、技能卡片与夜间模式的视觉表现，减少按钮、背景和布局不一致问题。

### Added

- 管理面板远程访问控制与默认远程访问运行模式。
- OpenClaw 版本检查与升级入口。
- 技能管理能力，包括内置技能识别、搜索、安装、卸载和已安装状态展示。
- 配置变更影响分析与保存前提示。
- Tool Profiles、环境变量配置、配置路径选择、PATH 配置等安装向导能力。
- 全量备份导入导出，以及升级时包含 memory 和插件数据的备份。
- 企业微信渠道支持与插件安装入口。

### Changed

- 管理面板顶部导航、系统设置区域、模型与渠道表单布局进行了多轮整理。
- 模型主模型切换改为点击模型卡片完成，并在卡片内展示主模型标记。
- 消息渠道卡片操作按钮改为贴底、居中、等宽并占满父容器。
- 技能卡片移除重复的内置 skill 名称等宽展示，补充统一元信息样式。
- 日志默认回溯上限提升到 1000 行。
- smoke test 增加可调的 fast-path 超时参数以缩短验证时间。

### Fixed

- 修复管理面板在复杂网络条件下的页面交付稳定性问题。
- 修复夜间模式下顶栏、消息渠道/模型添加区域以及部分表单组件背景异常。
- 修复模型测试加载失败、API 路径、协议与 `apiType` 处理不一致的问题。
- 修复配置导入文件选择器失效、异步校验调用异常以及配置保存/校验流程问题。
- 修复 Telegram 渠道缺少必要 `bot token` 字段校验的问题。
- 修复 QQ Bot / 企业微信插件安装后仍被判定为未安装、启用失败、状态检测失真的问题。
- 修复 `plugins.allow` 写入 npm 包名导致 `plugin not found` 的问题。
- 修复模型 `Base URL`、`密钥存储`、主模型按钮换行导致的卡片 UI 不一致问题。
- 修复空日志文件、npm 命令调用、provider 校验和模型 ID 校验等运行时细节问题。

### Internal

- 后端 HTTP、路由、静态资源、配置、网关、插件、备份等模块完成拆分与整理。
- 前端状态、配置编辑器逻辑和整体样式体系进一步模块化。
- 引入 OKLCH 设计 token 与更稳定的语义化颜色系统。
