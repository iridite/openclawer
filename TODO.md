- [x] 需要向 openclaw.json 中写入 `gateway.model= local`
- [x]（无用户允许的情况下，自主启动 gateway）并需要能从 openclaw.json 中主动读取 token `gateway.auth.token`
- [x] 检查 “配置摘要” 的真实性和实时更新能力
- [x] 快速操作部分，添加一个启动网关和停止网关的按钮
- [x] 在 “控制台” 页面添加一个板块，用于展示各类 log 输出的实际位置，方便用户自主查错。只是以等宽字体展示 log 的具体路径，用户可以直接复制路径去查看日志文件，没有功能作用。
- [x] 在配置编辑部分添加一个快速输入 provider 和 model 的功能，用户可以直接输入 provider 和 model 来快速配置一个模型（一个基于 openai / anthropic 协议的纯（完整）模型名称以及url和 api key 来配置的界面
- [x] 修复模型配置中的 api 参数问题，添加 API 类型下拉框，支持所有 OpenClaw 支持的 API 类型（openai-completions, anthropic-messages 等）
- [x] 使用内存，CPU 情况在仪表盘需要展示出来，方便用户监控网关的运行状态
- [x] 初始 openclaw json 配置的时候加上 reload hybrid 配置，减少用户修改配置文件的步骤
- [x] 安装过程卡在 40% 应该是在安装 npm 软件包，在 install wizard 中提醒一下
- [x] 通过 iframe + 反向代理实现 OpenClaw Dashboard 嵌入到管理界面中，解决远程访问时无法打开原生控制台的问题
- [x] 提供配置文件的导入和导出功能，方便用户备份和迁移配置
- [ ] 提供离线安装和在线安装的选项（后期可以实现离线安装的更新）
- [x] 修复模型配置无法删除的问题（模型报错不存在）
- [x] 修复模型配置编辑器报错 mode 相关的问题（需要调整即使不通过检查但也可以保存，只是给出警告）
- [ ] ？修复使用快速配置消息渠道由渠道 id 报错的问题
- [x] 优化模型配置的 tooltip，告知用户模型 provider 和 model_id 都必须是无空格的英文字符或者数字和点。
- [x] 解决连接原生面板时候的需要手动填入网关令牌的问题
- [ ] 除了自动刷新之外能不能实现哪种实时更新，即有更改，我们这边就能更新？（针对用户配置的模型以及消息渠道等？）
- [ ] 明暗 UI 切换
- [ ] （不做这个）要求在 dashboard 里面实现一个终端？可以根据 onboard 来进行直接的配置？
- [x] 添加一个按钮用于复制 openclaw.json 的内容
- [x] 修复点击模型，不会让当前激活模型变换的问题
- [x] 修复 tooltip 定位锚在tooltip ui 的中间，这会导致展示的 tooltip 会超出屏幕
- [x] 主界面名称修改，强调是 OC-Deploy，同时强调我们的面板名称和原生面板的区别（“进入控制台” -> “进入原生控制面板（dashboard）”）
- [x] 整体 UI 美化，去 emoji 化；对各个的子页面 tabs 进行重新设计和优化
- [x] 修复 UI 提示 “添加新模型” 但是实际按钮却是 “添加模型” 的问题
- [x] 添加一键恢复配置的功能
- [x] 修复 telegram 需要 allowFrom : [ "*" ] 的额外配置
- [x] 未选择渠道类型的时候，却展示了一个 Bot Token 的提示框
- [x] 修复 telegram 配置 template 的错误（多了一个 type 字段；搞错了渠道名称的位置）
- [-] QQ 消息渠道接入，需要处理插件的问题
- [x] 修复模型配置无法在创建后编辑的问题
- [ ] 对所有的 tooltip 进行一次完整详尽的优化，尽量让它真的能解决问题；另外针对某些特别重要的提示，就直接放在 UI 明面上。
- [ ] 快速添加模型的卡片 UI，将模型 ID 置于顶上，同时设为主体色；而模型的供应商设在下方。
---
{
  "channels": {
    "qqbot": {
      "enabled": true,
      "appId": "",
      "appSecret": "",
      "groupPolicy": "open"
    }
  }
}
